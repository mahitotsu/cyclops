{
  "scripts": {
    "build": "esbuild --bundle ./src/index.mjs --outdir=dist --platform=node --target=node18 --minify"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.470.0",
    "esbuild": "^0.19.9"
  }
}
